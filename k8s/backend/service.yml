apiVersion: v1 
kind: Service 
metadata:
  name: sewalink-backend-service 
spec:
  selector:
    app: sewalink-backend
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    nodePort: 30005
    targetPort: 5000
