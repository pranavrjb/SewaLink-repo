apiVersion: v1
kind: Service
metadata:
  name: sewalink-frontend-service
spec:
  selector:
    app: sewalink-frontend
  type: NodePort
  ports:  
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30006